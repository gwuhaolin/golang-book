{
  "name": "golang-book",
  "version": "0.0.1",
  "scripts": {
    "init": "npm i && gitbook install",
    "start": "gitbook",
    "build": "gitbook build . docs && touch docs/CNAME && echo 'go.wuhaolin.cn' > docs/CNAME"
  },
  "dependencies": {
    "gitbook-cli": "^2.3.2",
    "gitbook-plugin-ga": "^1.0.1"
  }
}
